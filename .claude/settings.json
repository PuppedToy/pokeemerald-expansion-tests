{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && node scripts/check-tracker.mjs || { echo 'Tracker inconsistent: fix tasks/bugs/indexes before finishing (see output above).' >&2; exit 2; }"
          }
        ]
      }
    ]
  }
}
