{
  "name": "pokeemerald-randomizer",
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "build": "node build.js",
    "start": "node build.js && node backend/server.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
