---
id: T-226
title: Frontend text/appearance batch (config descriptions, beta/legal copy, layout fixes)
status: in-progress     # proposed | in-progress | done | abandoned
type: docs              # feature | fix | refactor | docs | chore
created: 2026-07-26
updated: 2026-07-26
target-version: 0.7.0
links: [T-216, T-222, T-224]
blocked-by: []
---

# T-226 — Frontend text/appearance batch

## Context

Owner-supplied batch of frontend copy edits + a few appearance/structure fixes (source: the owner's
`texts.txt`). Mostly config-form option descriptions (`field-hint` / `toggle-desc` / `radio-card-desc` in
`frontend/js/config-form.js`), plus the held-beta / closed-beta / legal copy (`account.js`, `index.html`).

## Plan

**Text (rewrite or delete a description):** ~30 config-form descriptions shortened / clarified / removed
(battle format, wild encounters, mutations, evolution, type themes, space-center, team types, prices,
starters, nicknames, docs-visibility). Held-beta text + closed-beta notice + the two legal notes trimmed.

**Structure / appearance fixes:**
- Mutations: give the "which fields may change" spread options (stats/abilities/types/learnsets) breathing
  room; move the per-field **Advanced** panel inside the box + make it conditional on the box. Same for
  **Moves**. Remove the **"rebalance moves"** option.
- Nicknames: put **"different names per gender"** before **"lock gender per route"**.
- Load UI: drop the **"Config:"** label before `[Load Preset] | [Load from bundle]`; the bundle-upload line
  ("Or skip randomization…") gets a single `----` divider below it (not two around it), placed below the
  load-preset options.

**Tests:** update any assertion that pins a changed/removed string (config-review-completeness, docs-*, etc.);
keep the suites green.

Acceptance criteria:
- [x] All text edits from the batch applied to the SOURCE (`config-form.js`, `index.html`, `account.js`);
      `dist/` regenerated by the build, not hand-edited. (Editorial: normalized "pokemon" → "Pokémon" to
      match the app's consistent styling.)
- [x] Structural fixes: mutation + move "which fields" toggles spaced out (gap 20→32) with the **Advanced**
      panel moved INSIDE each box + made conditional on the master toggle; nickname "Different names per
      gender" moved before "Lock gender per route"; load-UI "Config:" label dropped; the "Or skip…" bundle
      line now has a single divider below it (auto-resolved by removing the Load-Preset hint above it).
- [ ] **DEFERRED — item "remove the option to rebalance moves" is ambiguous**: there is no toggle literally
      named "rebalance moves". Closest is the Pokémon-mutation "Mutate learnsets" toggle (removing it is a
      *functional* change). Awaiting owner confirmation before touching it.
- [x] Frontend suite green (209; the T-188 Load-hint test updated to the removed-hint reality); `shoot` no
      overflow. Verified the mutation/move restructure keeps balanced div nesting.
- [ ] Owner re-validates the copy/layout on the live site after deploy.

## Progress log

- **2026-07-26** — Task created from the owner's `texts.txt`. Located the strings (mostly inline HTML
  descriptions in `config-form.js`). Doing it by section so intertwined text + structure edits stay coordinated.

## Outcome

<!-- Filled when closing. -->
